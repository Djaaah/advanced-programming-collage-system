window.onload = function () {
    /* Adicionar Novo Aluno \/ 
    
    var inputNomeCompleto = document.querySelector("#nome-completo");
    
    inputNomeCompleto.addEventListener("keypress", function (e) {
        var keyCode = (e.keyCode ? e.keyCode : e.which);

        if (keyCode > 47 && keyCode < 58) {
            e.preventDefault();
        }
    });

    var matriculaAluno = document.querySelector('#matricula-aluno');
    matriculaAluno.value = "123456789";

     AddNewVoter /\ */
}
